 Code pen link: https://codepen.io/andy-sakai/pen/RwrJQZp

    1. What is Semantic HTML? 
	Semantic HTML is the use of HTML elements where the keyword clearly defines the function of the command.  An example can he <form>, <table>, or <article>

    2. What is HTML used for? 
	HTML is used to develop the core content of the page. HTML is where all the headings, the main body texts, and footer texts can be written and structured 

    3. What is an attribute and where do we put it? 
	An attribute provides additional information about HTML elements.  An example can be <a = href> where href is the HTML attribute to a

    4. What is the h1 tag used for? How many times should I use it on a page?
	The h1 tag is used to create the largest heading on the page.  This should technically be used once. 
	
    5. Name two tags that have required attributes
	<a> has a required attribute called href
	<img> has a required attribute called sec	

    6. What do we put in the head of our HTML document? 
	the <head> element is a container for metadata and is placed between the html tag and the body tag
	
    7. What is an id? 
	the id attribute specifies a unique id for the html element that points to a style and manipulates the style with a specific id

    8. What elements can I add an id to? 
	An id can be added onto any html element.

    9. How many times can I use the same id on a page? 
	id's can only be used once on a page since they are unique. 

    10. What is a class? 
	A class is a specific designation to a specific division of an html page that can be called in from your CSS.

    11. What elements can I add a class to? 
	the same class can be added to any element on the html page.

    12. How many times can I use the same class on a page? 
	Classes can be used as many times as needed	

    13. How do I get my link to open in a new tab?
	You have to add 'target=_blank" the code right after the URL inside of the <a> tag.

    14. What is the alt attribute in the image tag used for? 
	The alt attribute specifies an alternate text for an image if the image cannot be displayed.

    15. How do I reference an id?
	id's can be refenced in the CSS using #(id name).

    16. What is the difference between a section and a div
	Section refers to the content inside is grouped based on a theme where as a div can be a random dividion of the page.

    17. What is CSS used for? 
	CSS is used for styling the web pag by adding style to the html.	

    18. How to we select an element? Example - every h2 on the page
	we use an h2 selector as follows: 
		h2<content here>

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
	classes can be used to designate multiple different elements in the html where as an id is only used once 
	an example is I can use a class to specificy multiple h2's but I would probably use an id for an h1 given that I only have one h1.		

    20. How do we select classes in CSS?
	you write a period followed by the class name (.class)

    21. How do we select a p element with a single class of “human””?
	p .human<> (if the class human is nested in the p element of your html)

    22. What is a parent child selector? When would this be useful? 
	The parent child selector matches elements that are direct children of other elements 
	For example if we have child selector 
		div ol > li em {color: purple}
		em would only be matched if it followed the descendants in that child selector.

    23. How do you select all links within a div with the class of sidebar?
		div links .sidebar

    24. What is a pseudo selector?
		A selector that selects the element in a specific state.

    25. What do we use the change the spacing between lines?
		line-height: (input pt values)
	
    26. What do we use to change the spacing between letters?
		letter-spacing: (input em or px values)

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
		text-transform:(capitalize, lowercase, capitals)

    28. How do I add a 1px border around my div that is dotted and black?
		div<
			Border-style:dotted
			border-color:black

    29. How do I select everything on the page? 
		*{}

    30. How do I write a comment in CSS?
		/* coment */
		
    31. How do I find out what file I am in, when I am using the command line? 
		cd

    32. Using the command line - how do I see a list of files/folders in my current folder?
		ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
		rm this deletes a file permenantly without warning 

    34. Why should I use version control? 
		Easier to use and backs up all of your work
	
    35. How often should I commit to github?
		As frequently as possible tp ensure all wanted changes are in the file

    36. What is the command we would use to push our repo up to github? 
		git push 

    37. Walk me through Lambda's git flow. 
	-fork the assignment
	-add all collaborators 
	-create a branch 
	-create a pull request on github 

Stretch Questions

    1. What is the difference between an inline element and a block element? 
	Inline elements are within the element where are block elements can overlap between elements


    2. What happens when an element is positioned absolutely? 
	the element is position if fixed

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
	height:auto;
	width:(input px,em, or %);
 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
	Block: <address> <article> <aside>
	Inline: <a> <abbr>
	Inline - Block: <span>
	

    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
	The CSS box model is a box that wraps around every HTML element and is used as the fundemental understanding of the style layout in CSS
	To make allments respect a width we can use 
		*{
			margin:(input value);
		}
